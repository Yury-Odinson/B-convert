export type ConversationRequest = {
    first: string,
    second: string
};

export type InputProps = {
    value: number,
    currency: string,
    setValue: any,
    setCurrency: any
};

export type Currency = "USDT" | "BTC" | "ETH";
